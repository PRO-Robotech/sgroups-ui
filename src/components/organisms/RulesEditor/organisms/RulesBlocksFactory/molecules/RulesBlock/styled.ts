import styled from 'styled-components'
import { Form } from 'antd'

const FormItem = styled(Form.Item)`
  margin-top: 15px;
  margin-bottom: 5px;

  label {
    min-width: 150px;
  }
`

export const Styled = {
  FormItem,
}
