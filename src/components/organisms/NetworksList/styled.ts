import styled from 'styled-components'

const SelectedItemsText = styled.div`
  display: flex;
  align-items: center;
  font-size: 16px;
  line-height: 24px;
  margin-right: 8px;
`

export const Styled = {
  SelectedItemsText,
}
